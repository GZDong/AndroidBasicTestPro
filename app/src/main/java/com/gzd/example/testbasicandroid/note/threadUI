1.回调函数和handler都可以在子线程更新UI，不同是，后者更加方便，前者需要手动去创建线程类，创建接口，
  后者有固定的使用方法
2.AsyncTask则更加地方便，它将异步过程和UI直接联系起来，也表明它主要用于这一类情况，并将这一类情况的
  各个阶段封装成了函数，比如预前操作，后台开启线程操作，*后台线程的进度情况（UI），后台线程执行结束（UI），
  它的创建也是根据实际需要来设计的，比如这个后台任务需要什么参数，用什么方式展示进度，返回什么结果，这3者
  都是采用泛型来指定